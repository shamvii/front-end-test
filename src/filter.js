{/* <Button onClick={reverseData => this.setState({clicked: false})} /> 
class Button extends Component {
    render() {
      return (
        <div>
          <button type="submit" onClick={event => this.props.onClick(event.target.value) }>Sort</button>
        </div>
  
      )
    }
  } */}